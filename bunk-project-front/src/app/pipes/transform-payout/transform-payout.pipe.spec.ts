import { TransformPayoutPipe } from './transform-payout.pipe';

describe('TransformPayoutPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformPayoutPipe();
    expect(pipe).toBeTruthy();
  });
});
